# 📱 PLAN: สร้างแอพ To-Do List ด้วย React Native + Expo

> คู่มือฉบับสมบูรณ์สำหรับผู้ที่ไม่เคยทำแอพมือถือมาก่อน

---

## 📋 สารบัญ
1. [เกี่ยวกับโปรเจค](#เกี่ยวกับโปรเจค)
2. [การติดตั้งและเตรียมความพร้อม](#การติดตั้งและเตรียมความพร้อม)
3. [การรันโปรเจคครั้งแรก](#การรันโปรเจคครั้งแรก)
4. [โครงสร้างโปรเจค](#โครงสร้างโปรเจค)
5. [ขั้นตอนการพัฒนาแอพ](#ขั้นตอนการพัฒนาแอพ)
6. [การเทสและ Debug](#การเทสและ-debug)
7. [การ Build แอพ (APK/IPA)](#การ-build-แอพ-apkipa)
8. [Resources และเอกสาร](#resources-และเอกสาร)

---

## 🎯 เกี่ยวกับโปรเจค

### แอพที่จะสร้าง: **To-Do List App**
แอพจัดการรายการสิ่งที่ต้องทำ ที่มีฟีเจอร์:
- ✅ เพิ่มงานที่ต้องทำ
- ✅ แสดงรายการงานทั้งหมด
- ✅ เช็คงานที่ทำเสร็จแล้ว
- ✅ ลบงาน
- ✅ กรองงาน (ทั้งหมด/ทำแล้ว/ยังไม่ทำ)
- ✅ บันทึกข้อมูลถาวร (AsyncStorage)

### เทคโนโลยีที่ใช้
- **React Native**: Framework สำหรับสร้างแอพมือถือด้วย JavaScript
- **Expo**: เครื่องมือที่ทำให้การพัฒนาแอพง่ายขึ้น (ไม่ต้อง setup Android Studio/Xcode)
- **JavaScript/JSX**: ภาษาที่ใช้เขียน

---

## 🔧 การติดตั้งและเตรียมความพร้อม

### ขั้นตอนที่ 1: ติดตั้ง Expo Go บนมือถือ

**สำหรับ Android:**
1. เปิด Google Play Store
2. ค้นหา "Expo Go"
3. ติดตั้งแอพ
4. เปิดแอพและพร้อมใช้งาน

**สำหรับ iOS:**
1. เปิด App Store
2. ค้นหา "Expo Go"
3. ติดตั้งแอพ
4. เปิดแอพและพร้อมใช้งาน

### ขั้นตอนที่ 2: ตรวจสอบว่าติดตั้งครบแล้ว

เปิด Terminal/Command Prompt และรันคำสั่ง:

```bash
node --version    # ควรแสดง v22.11.0 (หรือใหม่กว่า)
npm --version     # ควรแสดง 11.7.0 (หรือใหม่กว่า)
```

✅ **หมายเหตุ**: คุณมี Node.js และ npm ติดตั้งไว้แล้ว!

---

## 🚀 การรันโปรเจคครั้งแรก

### ขั้นตอนที่ 1: เข้าไปในโฟลเดอร์โปรเจค

```bash
cd todo-app
```

### ขั้นตอนที่ 2: รัน Development Server

```bash
npm start
```

หรือ

```bash
npx expo start
```

### ขั้นตอนที่ 3: เปิดแอพบนมือถือ

หลังจากรันคำสั่งด้านบน จะเห็น:
1. **QR Code** แสดงใน Terminal
2. **URL** สำหรับเปิดแอพ

**วิธีเปิดบนมือถือ:**

**Android:**
1. เปิดแอพ Expo Go บนมือถือ
2. กด "Scan QR code"
3. สแกน QR code จาก Terminal
4. รอสักครู่ แอพจะโหลดและแสดงผล

**iOS:**
1. เปิดแอพ Camera บนมือถือ
2. ชี้ไปที่ QR code
3. กด notification ที่ขึ้นมา
4. แอพจะเปิดใน Expo Go

### ขั้นตอนที่ 4: ทดสอบว่าใช้งานได้

คุณควรเห็นหน้าจอแสดงข้อความ:
```
Open up App.js to start working on your app!
```

✅ **ถ้าเห็นข้อความนี้ = รันสำเร็จแล้ว!**

---

## 📁 โครงสร้างโปรเจค

```
todo-app/
├── App.js              # ไฟล์หลักของแอพ (เริ่มเขียนโค้ดที่นี่)
├── app.json            # ไฟล์ config ของ Expo
├── package.json        # รายการ dependencies และ scripts
├── assets/             # โฟลเดอร์เก็บรูปภาพ/ไอคอน
├── node_modules/       # ไลบรารีที่ติดตั้ง (ไม่ต้องแก้ไข)
└── .gitignore          # ไฟล์ที่ไม่ต้อง commit ใน git
```

### ไฟล์สำคัญ

#### `App.js` - ไฟล์หลักที่เราจะแก้ไข
```javascript
import { StatusBar } from 'expo-status-bar';
import { StyleSheet, Text, View } from 'react-native';

export default function App() {
  return (
    <View style={styles.container}>
      <Text>Open up App.js to start working on your app!</Text>
      <StatusBar style="auto" />
    </View>
  );
}

const styles = StyleSheet.create({
  container: {
    flex: 1,
    backgroundColor: '#fff',
    alignItems: 'center',
    justifyContent: 'center',
  },
});
```

#### `package.json` - Scripts ที่ใช้งาน
```json
{
  "scripts": {
    "start": "expo start",        // รัน development server
    "android": "expo start --android",  // รันบน Android emulator
    "ios": "expo start --ios",          // รันบน iOS simulator
    "web": "expo start --web"           // รันบน browser (สำหรับทดสอบ)
  }
}
```

---

## 🛠️ ขั้นตอนการพัฒนาแอพ

### Phase 1: ติดตั้ง Dependencies เพิ่มเติม

```bash
# สำหรับบันทึกข้อมูลถาวร
npx expo install @react-native-async-storage/async-storage

# สำหรับไอคอนสวยงาม
npx expo install @expo/vector-icons
```

### Phase 2: สร้าง UI Components

**สิ่งที่ต้องสร้าง:**

1. **Header Component**
   - แสดงชื่อแอพ
   - แสดงจำนวนงานทั้งหมด

2. **Input Component**
   - TextInput สำหรับพิมพ์งานใหม่
   - ปุ่ม "เพิ่ม" งาน

3. **TaskItem Component**
   - แสดงชื่องาน
   - Checkbox สำหรับเช็คว่าทำเสร็จ
   - ปุ่มลบงาน

4. **TaskList Component**
   - แสดงรายการงานทั้งหมด
   - ใช้ FlatList หรือ ScrollView

5. **Filter Component**
   - ปุ่มกรอง: ทั้งหมด / ทำแล้ว / ยังไม่ทำ

### Phase 3: State Management

**State ที่ต้องจัดการ:**

```javascript
const [tasks, setTasks] = useState([]);           // รายการงานทั้งหมด
const [inputText, setInputText] = useState('');   // ข้อความที่พิมพ์
const [filter, setFilter] = useState('all');      // ฟิลเตอร์ปัจจุบัน
```

**โครงสร้างข้อมูล Task:**
```javascript
{
  id: '1',              // unique ID
  text: 'ซื้อของ',      // ชื่องาน
  completed: false,     // สถานะทำเสร็จ
  createdAt: Date.now() // เวลาที่สร้าง
}
```

### Phase 4: Functions ที่ต้องเขียน

```javascript
// 1. เพิ่มงานใหม่
const addTask = (text) => { ... }

// 2. ลบงาน
const deleteTask = (id) => { ... }

// 3. Toggle สถานะงาน (ทำเสร็จ/ยังไม่ทำ)
const toggleTask = (id) => { ... }

// 4. บันทึกข้อมูลลง AsyncStorage
const saveTasks = async (tasks) => { ... }

// 5. โหลดข้อมูลจาก AsyncStorage
const loadTasks = async () => { ... }

// 6. กรองงานตาม filter
const getFilteredTasks = () => { ... }
```

### Phase 5: เพิ่ม AsyncStorage (บันทึกข้อมูล)

AsyncStorage ทำให้ข้อมูลไม่หายเมื่อปิดแอพ

```javascript
import AsyncStorage from '@react-native-async-storage/async-storage';

// บันทึก
const saveTasks = async (tasks) => {
  try {
    await AsyncStorage.setItem('tasks', JSON.stringify(tasks));
  } catch (error) {
    console.error('Error saving tasks:', error);
  }
};

// โหลด
const loadTasks = async () => {
  try {
    const tasks = await AsyncStorage.getItem('tasks');
    return tasks ? JSON.parse(tasks) : [];
  } catch (error) {
    console.error('Error loading tasks:', error);
    return [];
  }
};
```

### Phase 6: Styling (แต่งหน้าตา)

ใช้ `StyleSheet` จาก React Native:

```javascript
const styles = StyleSheet.create({
  container: {
    flex: 1,
    backgroundColor: '#f5f5f5',
    paddingTop: 50,
  },
  header: {
    backgroundColor: '#6200ee',
    padding: 20,
    alignItems: 'center',
  },
  // ... styles อื่นๆ
});
```

---

## 🧪 การเทสและ Debug

### Hot Reload
- แก้ไขโค้ดใน `App.js` แล้ว **Save**
- แอพบนมือถือจะ **รีโหลดอัตโนมัติ**
- ไม่ต้องกด Run ใหม่!

### วิธีดู Console Log

**ใน Terminal:**
```javascript
console.log('Hello from app!');
```
ข้อความจะแสดงใน Terminal ที่รัน `npm start`

**ใน Expo Go:**
- เขย่ามือถือ (shake)
- เลือก "Show/Hide Dev Menu"
- กด "Debug Remote JS" เพื่อเปิด Chrome DevTools

### แก้ไข Error ทั่วไป

**Error: Cannot find module**
```bash
npm install
```

**Error: Port already in use**
```bash
# Kill process ที่ใช้ port 8081
# หรือรันด้วย
npx expo start --port 8082
```

**แอพไม่อัพเดท**
```bash
# ปิด server แล้วรันใหม่
# กด Ctrl+C แล้วรัน
npm start
```

---

## 📦 การ Build แอพ (APK/IPA)

### วิธีที่ 1: Build ด้วย EAS (แนะนำ)

**ขั้นตอน:**

1. **ติดตั้ง EAS CLI**
```bash
npm install -g eas-cli
```

2. **Login เข้า Expo**
```bash
eas login
```
(สร้างบัญชีฟรีที่ https://expo.dev/)

3. **Configure โปรเจค**
```bash
eas build:configure
```

4. **Build APK สำหรับ Android**
```bash
# Build แบบ APK (สำหรับแจกจ่าย)
eas build -p android --profile preview
```

5. **รอให้ Build เสร็จ**
- ระบบจะ build บน cloud
- รอประมาณ 10-20 นาที
- ดาวน์โหลดไฟล์ APK จาก URL ที่ได้

6. **โหลด APK ไปติดตั้ง**
- คัดลอก APK ไปมือถือ Android
- เปิดไฟล์และติดตั้ง
- (อาจต้องอนุญาต "Install from Unknown Sources")

### วิธีที่ 2: Build Local (ยาก - ต้องมี Android Studio)

**สำหรับ Android:**
```bash
# Eject จาก Expo
npx expo prebuild

# Build APK
cd android
./gradlew assembleRelease

# ไฟล์ APK จะอยู่ที่:
# android/app/build/outputs/apk/release/app-release.apk
```

**หมายเหตุ:** วิธีนี้ซับซ้อน ควรใช้ EAS Build แทน

### วิธีที่ 3: สำหรับส่งงาน/Demo

**ใช้ Expo Go (ไม่ต้อง Build)**
- แชร์ลิงก์ Expo: `exp://xxx.xxx.xxx.xxx:8081`
- อาจารย์/เพื่อนติดตั้ง Expo Go แล้วสแกน QR code
- **ข้อจำกัด**: ต้องอยู่ใน network เดียวกัน

**Build เป็น APK และแชร์ไฟล์**
- Build ด้วย EAS (วิธีที่ 1)
- อัพโหลด APK ไปที่ Google Drive
- แชร์ลิงก์ให้อาจารย์ดาวน์โหลด

---

## 📚 Resources และเอกสาร

### เอกสารอย่างเป็นทางการ
- [Expo Documentation](https://docs.expo.dev/)
- [React Native Docs](https://reactnative.dev/docs/getting-started)
- [React Hooks Guide](https://react.dev/reference/react)

### Components ที่ใช้บ่อย
- [View](https://reactnative.dev/docs/view) - Container พื้นฐาน
- [Text](https://reactnative.dev/docs/text) - แสดงข้อความ
- [TextInput](https://reactnative.dev/docs/textinput) - รับ input
- [TouchableOpacity](https://reactnative.dev/docs/touchableopacity) - ปุ่มกด
- [FlatList](https://reactnative.dev/docs/flatlist) - แสดงรายการ
- [ScrollView](https://reactnative.dev/docs/scrollview) - เลื่อนหน้าจอ

### Libraries ที่น่าสนใจ
- [AsyncStorage](https://docs.expo.dev/versions/latest/sdk/async-storage/) - บันทึกข้อมูล
- [@expo/vector-icons](https://icons.expo.fyi/) - ไอคอนฟรี
- [React Native Paper](https://callstack.github.io/react-native-paper/) - UI Components สวย

### Tutorial Videos (แนะนำ)
- [React Native Tutorial for Beginners](https://www.youtube.com/results?search_query=react+native+tutorial+beginner)
- [Expo Tutorial](https://www.youtube.com/results?search_query=expo+tutorial)

---

## ✅ Checklist สำหรับส่งงาน

### ส่วนที่ 1: พัฒนาแอพ
- [ ] รันโปรเจคได้บนมือถือ
- [ ] เพิ่มงานได้
- [ ] ลบงานได้
- [ ] เช็คงานเสร็จได้
- [ ] กรองงานได้ (ทั้งหมด/ทำแล้ว/ยังไม่ทำ)
- [ ] บันทึกข้อมูลถาวร (AsyncStorage)
- [ ] UI ดูดีและใช้งานง่าย

### ส่วนที่ 2: Documentation
- [ ] README.md อธิบายวิธีรันโปรเจค
- [ ] Screenshot หรือ Video Demo
- [ ] อธิบายฟีเจอร์ที่มี

### ส่วนที่ 3: ส่งงาน
- [ ] Build เป็น APK (Android) หรือใช้ Expo Link
- [ ] อัพโหลด Code ขึ้น GitHub
- [ ] ทดสอบว่าติดตั้งได้จริง
- [ ] เตรียม Presentation สำหรับนำเสนอ

---

## 🎓 Tips สำหรับนักศึกษา

### 1. เริ่มจากง่ายไปยาก
- วันที่ 1: รันโปรเจคให้ได้ + ทำความเข้าใจ React Native
- วันที่ 2-3: สร้าง UI พื้นฐาน (Input, List, Button)
- วันที่ 4-5: เพิ่ม Logic (Add, Delete, Toggle)
- วันที่ 6: เพิ่ม AsyncStorage
- วันที่ 7: แต่ง UI ให้สวย + Build APK

### 2. ใช้ Console.log เยอะๆ
```javascript
console.log('Current tasks:', tasks);
console.log('Input text:', inputText);
```

### 3. ทดสอบบ่อยๆ
- แก้โค้ดนิดหน่อยแล้วทดสอบทันที
- อย่ารอเขียนเสร็จหมดค่อยทดสอบ

### 4. Google เป็นเพื่อน
- "How to add checkbox in React Native"
- "React Native FlatList example"
- "AsyncStorage React Native tutorial"

### 5. Backup Code
```bash
git init
git add .
git commit -m "Initial commit"
git remote add origin https://github.com/yourusername/todo-app.git
git push -u origin main
```

---

## 🚨 ปัญหาที่พบบ่อย

### 1. มือถือเชื่อมต่อ Expo ไม่ได้
**วิธีแก้:**
- ตรวจสอบว่าคอมและมือถืออยู่ Wi-Fi เดียวกัน
- ปิด VPN ถ้าเปิดไว้
- ใช้ "Tunnel" แทน "LAN" ใน Expo DevTools

### 2. แอพโหลดช้า
**วิธีแก้:**
- ครั้งแรกจะโหลดช้า ครั้งต่อไปจะเร็วขึ้น
- ลองเปลี่ยน Wi-Fi หรือใช้ 4G/5G

### 3. Build APK ไม่ได้
**วิธีแก้:**
- ใช้ EAS Build แทน Local Build
- ตรวจสอบว่า Login Expo แล้ว
- อ่าน Error Message ให้ละเอียด

### 4. App Crash บนมือถือ
**วิธีแก้:**
- เปิด DevTools ดู Error
- ตรวจสอบ Syntax Error
- ลอง Clear Cache: `npx expo start -c`

---

## 📞 ขอความช่วยเหลือ

### ถ้าติดปัญหา:
1. อ่าน Error Message ใน Terminal
2. Google Error Message
3. ถาม ChatGPT/Claude
4. ดูตัวอย่างโค้ดใน [Expo Examples](https://github.com/expo/examples)
5. ถาม Claude Code อีกครั้ง! 😊

---

## 🎉 ขั้นตอนถัดไป

หลังจากอ่าน PLAN.md นี้แล้ว:

1. **ทดสอบรันโปรเจค**: `npm start`
2. **เปิดแอพบนมือถือ**: สแกน QR code
3. **เริ่มเขียนโค้ด**: แก้ไข `App.js`
4. **ขอความช่วยเหลือ**: ถามได้ตลอดเวลา!

---

**💡 หมายเหตุสำคัญ:**
- อย่ากลัวที่จะลองผิดลองถูก
- การทำแอพมือถือไม่ยากอย่างที่คิด
- ทำทีละขั้นตอน ค่อยๆ ไป จะสำเร็จแน่นอน!

**Good luck! 🚀**
